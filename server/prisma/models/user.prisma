model User {
  id         String          @id @default(uuid())
  username   String          @unique
  lastActive DateTime?
  createdAt  DateTime        @default(now())
  updatedAt  DateTime        @updatedAt

  documents  Document[]      @relation("AuthorDocs")

  chat       Chat[]

  editedDocs DocEditor[]

  @@map("users")
}